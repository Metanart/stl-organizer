import { BaseTranslation } from '../../i18n-types.generated'

const enConfig: BaseTranslation = {
  form: {
    outputFolder: 'Output folder',
    tempFolder: 'Temp Folder',
    maxThreads: 'Max Threads',
    autoProcessOnScan: 'Auto Process on Scan',
    autoArchiveOnComplete: 'Auto Archive on Complete',
    useMultithreading: 'Use Multithreading',
    debugMode: 'Debug Mode'
  }
}

export default enConfig
