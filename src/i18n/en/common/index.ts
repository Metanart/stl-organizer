import { BaseTranslation } from '../../i18n-types.generated'

const enCommon: BaseTranslation = {
  actions: {
    cancel: 'Cancel',
    save: 'Save'
  },
  dbErrors: {
    notUnique: 'The value is not unique for "{{columns}}"'
  }
}

export default enCommon
