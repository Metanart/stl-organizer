import { BaseTranslation } from '../../i18n-types.generated'

const enApp: BaseTranslation = {
  navigation: {
    home: 'Home',
    models: 'Models',
    sources: 'Sources',
    tasks: 'Tasks',
    config: 'Config'
  }
}

export default enApp
