export enum Actions {
  SCANNED = 'scanned',
  MOVED = 'moved',
  RENAMED = 'renamed',
  ARCHIVED = 'archived',
  DELETED = 'deleted',
  ERROR = 'error',
  HASHED = 'hashed',
  NORMALIZED = 'normalized'
}
